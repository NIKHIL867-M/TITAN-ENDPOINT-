<UserControl x:Class="TitanEndpoint.App.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardStyle}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="TitleText" Style="{StaticResource PageTitleStyle}" HorizontalAlignment="Center"/>
            <TextBlock Text="This page has not been built yet in this pass." Style="{StaticResource SecondaryTextStyle}"
                       Margin="0,8,0,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>
